@using Syncfusion.Server.Base;
@using Syncfusion.Server.Base.DataClasses;

@{
    Layout = null;
    var imageHelper = new ImageHelper();
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@GlobalAppSettings.BrandingDisplayName - @T["Sign in"]</title>
    <link rel="icon" href="@GlobalAppSettings.CdnDomain/static/company/@(GlobalAppSettings.Branding == AppBranding.Bold.Identifier ? "boldid" : GlobalAppSettings.Branding)/logo/v1/@IconFileNames.Favicon" />
    @await Html.PartialAsync("../Shared/_FontReference")
    @await Html.PartialAsync("../Shared/_ThemeReference")
    <link rel="stylesheet" href="~/bundles/css/error-page.css" integrity="@IntegrityTagHelper.GetIntegrity("bundles/css/error-page.css")" crossorigin="anonymous" asp-append-version="true" />
</head>
<body>
    <div class="login-container-bg">
        <div class="login-conatiner">
            <div class="login-main-div">
                <div class="login-logo">
                    <img src="@imageHelper.LoginFailedImage()" alt="@T["Login Failed"]" />
                </div>
                <div class="login-secondary-div">
                    <div class="login-page-title">
                        @T["Login Failed"]
                    </div>
                    <div class="login-page-content">@T["Sorry, we are having trouble logging you in."]</div>
                    <div class="support-div">
                        <a href="@Url.Action("Login", "Accounts")" class="login-button">
                            @T["Retry"]
                        </a>
                    </div>
                    <div class="external-container">
                        <div class="divider-container col-xs-12">
                            <div class="line col-xs-4"></div>
                        </div>
                        <div class="alternative-login">
                            <div class="page-wrapper">
                                <span style="float:left">@T["Still having trouble?"]</span>
                                <span class="hoverlink" style="float:right">
                                    <a href="@CloudAppConfig.ContactUsUrl(TenantType.BoldBI)">
                                        @T["Contact Support"]
                                    </a>
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>